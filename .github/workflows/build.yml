name: build

on:
  schedule:
    - cron: '0 9 * * *' # 06:00 BRT
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm test

      # Se o provider falhar, o build falha ANTES do deploy e o dist/
      # anterior continua no ar. Nunca publicar feed vazio: apagaria a
      # agenda de todo mundo que assinou.
      - run: pnpm run build
        env:
          API_FUTEBOL_TOKEN: ${{ secrets.API_FUTEBOL_TOKEN }}

      # O commit diário do state.json também impede o GitHub de desativar
      # o cron por inatividade (60 dias sem commit em repo público).
      - name: Commit state.json se mudou
        run: |
          if ! git diff --quiet -- data/state.json; then
            git config user.name 'github-actions[bot]'
            git config user.email 'github-actions[bot]@users.noreply.github.com'
            git add data/state.json
            git commit -m 'chore: atualiza state.json [skip ci]'
            git push
          fi

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
